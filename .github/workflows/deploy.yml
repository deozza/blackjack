name: Deploy Workflow

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: echo "This is a test step in the deploy workflow."